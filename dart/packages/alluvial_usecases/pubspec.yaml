name: alluvial_usecases
description: The application layer — one use case per thing a caller can ask for.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  alluvial_domain:
    path: ../alluvial_domain
  equatable: ^2.0.7
  fpdart: ^1.2.0

dev_dependencies:
  alluvial_store_memory:
    path: ../alluvial_store_memory
  alluvial_testing:
    path: ../alluvial_testing
  lints: ^6.0.0
  shouldly: ^0.5.0+1
  test: ^1.25.0
