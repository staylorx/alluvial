name: alluvial_store_yaml
description: A folder of YAML files as the story store.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  alluvial_domain:
    path: ../alluvial_domain
  fpdart: ^1.2.0
  path: ^1.9.0
  yaml: ^3.1.3

dev_dependencies:
  alluvial_testing:
    path: ../alluvial_testing
  lints: ^6.0.0
  shouldly: ^0.5.0+1
  test: ^1.25.0
