name: alluvial_render
description: The SVG engines — the braid chart and the two-clock chart — and the builder that lays a story out.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  alluvial_domain:
    path: ../alluvial_domain
  equatable: ^2.0.7

dev_dependencies:
  alluvial_store_yaml:
    path: ../alluvial_store_yaml
  lints: ^6.0.0
  shouldly: ^0.5.0+1
  test: ^1.25.0
