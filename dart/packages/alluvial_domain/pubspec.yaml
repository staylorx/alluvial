name: alluvial_domain
description: Entities, failures and contracts for the alluvial story store.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  equatable: ^2.0.7
  fpdart: ^1.2.0

dev_dependencies:
  lints: ^6.0.0
  shouldly: ^0.5.0+1
  test: ^1.25.0
