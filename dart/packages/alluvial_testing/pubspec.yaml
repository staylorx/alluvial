name: alluvial_testing
description: The shared repository contract suite every store adapter must pass.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  alluvial_domain:
    path: ../alluvial_domain
  fpdart: ^1.2.0
  shouldly: ^0.5.0+1
  test: ^1.25.0

dev_dependencies:
  lints: ^6.0.0
